:root{
  --blue1:#0d4aa5;
  --blue2:#2b7bd7;
  --blue3:#0a2e6e;
  --bg:#f2f6fb;
  --card:#ffffff;
  --line:#dbe6f6;
  --text:#222;
  --muted:#666;
}

*{box-sizing:border-box;}
body{margin:0;font-family:"Segoe UI",Arial,sans-serif;background:var(--bg);color:var(--text);}

.topbar{
  background:linear-gradient(180deg,var(--blue2),var(--blue1));
  color:#fff;
  padding:18px 12px;
}
.topbar-inner{max-width:1100px;margin:0 auto;display:flex;align-items:center;justify-content:space-between;}
.brand-title{font-size:22px;font-weight:700;letter-spacing:1px;}
.brand-sub{font-size:12px;opacity:.9;margin-top:3px;}

.nav{background:var(--blue3);}
.nav-inner{max-width:1100px;margin:0 auto;display:flex;gap:10px;flex-wrap:wrap;padding:10px 12px;}
.nav-item{color:#fff;text-decoration:none;padding:6px 10px;border-radius:6px;font-size:14px;}
.nav-item:hover{background:rgba(255,255,255,.15);}

.banner{max-width:1100px;margin:12px auto 0;position:relative;border-radius:10px;overflow:hidden;border:1px solid var(--line);background:#000;}
.banner img{width:100%;height:230px;object-fit:cover;display:block;}
.banner-mask{position:absolute;inset:0;background:linear-gradient(90deg,rgba(0,0,0,.45),rgba(0,0,0,.05));display:flex;align-items:center;}
.banner-text{padding-left:26px;color:#fff;}
.banner-big{font-size:46px;font-weight:700;letter-spacing:6px;}
.banner-small{font-size:22px;margin-top:6px;opacity:.95;letter-spacing:8px;}

.container{max-width:1100px;margin:12px auto;padding:0 12px 30px;}
.grid{display:grid;grid-template-columns:1.2fr 1fr 0.9fr;gap:14px;}
@media(max-width:980px){.grid{grid-template-columns:1fr;} .banner img{height:180px;} .banner-big{font-size:34px;}}

.panel{background:var(--card);border:1px solid var(--line);border-radius:10px;overflow:hidden;}
.panel-hd{display:flex;justify-content:space-between;align-items:center;padding:10px 12px;background:linear-gradient(180deg,#e9f2ff,#d7e8ff);}
.panel-hd h2{margin:0;font-size:16px;color:var(--blue3);}
.more{color:var(--blue1);text-decoration:none;font-size:12px;}
.more:hover{text-decoration:underline;}

.panel-bd{padding:12px;}
.list{margin:0;padding:0;list-style:none;}
.list li{padding:7px 0;border-bottom:1px dashed #e7eefc;font-size:13px;line-height:1.35;}
.list li:last-child{border-bottom:none;}
.dot{color:var(--blue1);margin-right:8px;font-weight:700;}

.intro{display:flex;gap:12px;}
@media(max-width:980px){.intro{flex-direction:column;}}
.intro-img{width:210px;height:150px;object-fit:cover;border-radius:10px;border:1px solid #eee;background:#f7f7f7;}
.intro-text p{margin:0 0 10px 0;font-size:13px;color:#333;line-height:1.6;}
.bullet{margin:0;padding-left:18px;font-size:13px;color:#333;}
.bullet li{margin:6px 0;}

.footer{margin-top:16px;text-align:center;color:#333;font-size:13px;}
.footer .muted{color:var(--muted);margin-top:6px;}
